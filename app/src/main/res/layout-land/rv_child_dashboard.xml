<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto" android:layout_width="match_parent"
        android:layout_height="wrap_content"> <!--Separación de altura entre tareas-->

    <!-- Check icon if task is finished -->
    <ImageView
            android:id="@+id/set_completed"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentStart="true"
            android:layout_alignParentLeft="true"
            android:src="@drawable/check"
            android:visibility="gone"
    />

    <!-- Task name -->
    <TextView
            android:id="@+id/tv_todo_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="30dp"
            android:textColor="#000000"
            app:fontFamily="sans-serif"
            android:onClick="buttonClick"
            android:layout_toRightOf="@+id/set_completed"
            android:layout_toEndOf="@+id/set_completed"/>

    <!-- Show menu options for a task -->
    <ImageView
            android:id="@+id/iv_menu"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:src="@drawable/ic_vertical_dot"
            android:padding="8dp"
    />
</RelativeLayout>


